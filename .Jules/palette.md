## 2024-05-22 - [Weekly Calendar Accessibility]
**Learning:** Custom visualizations like the weekly dots (colors/symbols) are invisible to screen readers without explicit text alternatives.
**Action:** Always map status codes to human-readable strings and attach them via `aria-label` and `title` for dual accessibility (screen readers + mouse hover).
